import { GridDeleteButtonDirective } from './grid-delete-button.directive';

describe('GridDeleteButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new GridDeleteButtonDirective();
    expect(directive).toBeTruthy();
  });
});
