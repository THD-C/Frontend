import { GridEditButtonDirective } from './grid-edit-button.directive';

describe('GridEditButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new GridEditButtonDirective();
    expect(directive).toBeTruthy();
  });
});
