import { ApiError } from '../../shared/models/error.model';

export const errors: ApiError = {
  'operation_failed': $localize`:@@statistics-errors.Operation-failed-Please-try-again:Operation failed. Please try again`,
}
