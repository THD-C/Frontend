import { ApiError } from '../../shared/models/error.model';

export const errors: ApiError = {
  'email_or_username_occupied': 'E-mail or username already occupied',
  'invalid_credentials': 'Credentials are invalid. Please try again or create password',
}