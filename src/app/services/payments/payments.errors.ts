import { ApiError } from '../../shared/models/error.model';

export const errors: ApiError = {
  'not_supported_currency': $localize`:@@payments-errors.Currency-not-supported:Currency not supported`,
  'invalid_nominal': $localize`:@@payments-errors.Invalid-nominal:Invalid-nominal`,
}
