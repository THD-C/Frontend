import { ApiError } from '../../shared/models/error.model';

export const errors: ApiError = {
  'invalid_current_password': $localize`:@@users-errors.Invalid-current-password:Invalid current password`,
}
